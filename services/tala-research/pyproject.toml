[project]
name = "tala-research"
version = "0.1.0"
description = "TALA Research Lab backend — FastAPI + Agent Reach + OpenRouter"
requires-python = ">=3.12"

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "httpx>=0.27",
]

[project.scripts]
tala-research = "main:app"