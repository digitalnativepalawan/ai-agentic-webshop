# Server-only application secrets. Keep values blank in source control.
ADMIN_PASSKEY=
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Self-hosted Postiz backend URL, for example https://postiz.example.com.
# The application appends /public/v1 unless it is already present.
POSTIZ_API_URL=
POSTIZ_API_KEY=
